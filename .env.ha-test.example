# Disposable Home Assistant Container + MQTT test environment defaults.
# Copy to .env.ha-test before using the ha-test-* Make targets.

HA_TEST_HTTP_BIND=127.0.0.1
HA_TEST_HTTP_PORT=8123
HA_TEST_MQTT_BIND=127.0.0.1
HA_TEST_MQTT_PORT=1883
HA_TEST_TZ=UTC
